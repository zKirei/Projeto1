{
  "name": "sghss",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress open",
    "start": "live-server --port=3000 --mount=/:public"
  },
  "devDependencies": {
    "cypress": "^14.3.3",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
